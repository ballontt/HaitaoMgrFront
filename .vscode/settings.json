{
    "typescript.tsdk": "./node_modules/typescript/lib",
    "files.exclude": {
        // "node_modules": true,
        "**/*.js": { "when": "$(basename).ts"},
        "**/*.js.map": { "when": "$(basename)"}
    }
}